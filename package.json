{
  "name": "kibi-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "make dev",
    "lint": "make lint",
    "test": "make test",
    "db_migrate": "docker-compose run --rm api alembic upgrade head"
  },
  "dependencies": {
    "@heroicons/react": "^2.0.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.3",
    "tailwindcss": "^3.4.17"
  }
}
